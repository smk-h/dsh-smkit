# dependencies
node_modules/

# build output (regenerated by `pnpm build` / `prepare`)
lib/

# typescript incremental state
*.tsbuildinfo

# package tarballs from `pnpm pack`
*.tgz

# os / editor noise
.DS_Store
Thumbs.db

# local scratch: 测试脚本的日志等一次性产物（如 ssh-stub-mcp.mjs --log=.tmp/ssh-stub.log）
.tmp/
