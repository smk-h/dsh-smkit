/**
 * English dictionary for the `custom-settings` locale namespace.
 *
 * The same key set as `zh.ts`, which `test/verify.mjs` asserts key by key: a
 * label added to one side and forgotten on the other would otherwise ship as
 * the raw key.
 */

import type { LocaleDict } from '../../../platform/types'

export const CUSTOM_SETTINGS_LOCALE_EN: LocaleDict = {
  "sectionLabel": "Custom settings",
  "sectionIntro": "DSH settings worth tuning on their own, without reaching for the configuration file: one tab per area, written into dsh's own settings.yaml and applied on save — no restart.",
  "tabRetry": "Model retry",
  "tabModelInput": "Model input",
  "tabOther": "Other settings",
  "otherIntro": "The settings this page is lined up to take next: each entry names where it lives in dsh, and each becomes editable here the way Model retry is.",
  "otherPlanned": "Planned",
  "otherNote": "Until then the value can be edited in dsh's settings.yaml, applied on save.",
  "plannedToolCalls": "Parallel tool calls",
  "plannedToolCallsHelp": "How many tool calls one reply may run at once (default 10). Raising it speeds up multi-tool work and makes a provider's rate limits likelier.",
  "plannedShellLimits": "Command timeout and output cap",
  "plannedShellLimitsHelp": "How long a foreground command may run (default 120 s), and how much of its output reaches the model (default 64 KB).",
  "plannedStreamIdle": "Model stream idle timeout",
  "plannedStreamIdleHelp": "How long a model stream may stay silent before it counts as timed out; slow-thinking, slow-first-byte endpoints need more.",
  "plannedRequestBudget": "Per-request image and file budget",
  "plannedRequestBudgetHelp": "How many images one request may carry and how large images and files may get before they are offloaded and referenced instead.",
  "plannedSearchUses": "Web search calls",
  "plannedSearchUsesHelp": "How many web searches one answer may run (default 5).",
  "plannedSubagentModels": "Models subagents may use",
  "plannedSubagentModelsHelp": "Which models the main agent may pick for subagents; off leaves them on the session's own model.",
  "modelInputIntro": "Choose which kinds of input each model of a route takes: dsh hands images only to a model whose image box is ticked. The edit is written into the route's own configuration and takes effect on the next request — no restart.",
  "modelInputNote": "\"Reset to default\" hands this row's capability back to dsh: a model dsh's built-in model list describes takes the capabilities that list records, one it does not take the route's own default, which is usually text only — so a default row is not automatically an imageless one. Allowing images also lets this plugin's MCP tools send images through the same choice.",
  "countModels": "{count} models",
  "modelEmpty": "No provider route is registered. Configure a provider on the Models page and the models it serves appear here.",
  "modelLoadFailed": "Could not read the model capabilities (HTTP {status})",
  "modelUnavailableSettings": "This deployment mounts no settings service: capabilities are readable but cannot be saved.",
  "noModels": "This route reports no models.",
  "choiceInherit": "Reset to default",
  "modelAdvanced": "Input options",
  "modalityText": "text",
  "modalityImage": "image",
  "modelStatusDefault": "default",
  "modelStatusCustom": "custom",
  "summaryVisual": "{count} take images",
  "refusalOtherAdapter": "This route is configured by the \"{ns}\" adapter, whose model fields are not the ones this page writes, so it is read-only here.",
  "refusalNoAddress": "This provider declares no editable settings address, so its capabilities are read-only here.",
  "refusalNoNamespace": "The \"{ns}\" namespace this route belongs to is not registered in this deployment, so nothing can be written.",
  "refusalNoModelList": "This deployment publishes no model list for this route, so there is nothing to configure.",
  "refusalNoStore": "This route stores its models where this page may not write: a route with its own models list has to be edited in that list, and overrides only reach a route whose models come from dsh's built-in list and which lists no models of its own.",
  "retryIntro": "Configure automatic retries for model-request failures on every registered provider route: mode, attempt count, and backoff. Saving applies to the next failure.",
  "heading": "Provider routes",
  "countRoutes": "{count} routes",
  "empty": "No provider route is registered. Configure a provider on the Models page and its retry policy appears here.",
  "unavailableLlm": "This deployment mounts no llm service, so the registered provider routes cannot be read.",
  "unavailableSettings": "This deployment mounts no settings service: policies are readable but cannot be saved.",
  "loadFailed": "Could not read the retry policies (HTTP {status})",
  "policyCustom": "Custom",
  "policyDefault": "Default",
  "edit": "Configure",
  "readOnly": "This provider declares no editable settings address, so its policy is read-only here.",
  "editTitle": "Configure the retry policy",
  "provider": "Route",
  "mode": "Mode",
  "modeNormal": "normal (bounded)",
  "modeAlways": "always (unbounded)",
  "maxRetries": "Max retries",
  "maxRetriesHelp": "Retries after the first request; 0 means no retry.",
  "unlimitedHelp": "always mode has no attempt limit: it stops on success, cancellation, or plugin disposal.",
  "initialDelayMs": "Initial delay (ms)",
  "maxDelayMs": "Max delay (ms)",
  "jitterRatio": "Jitter ratio (0 to 1)",
  "backoffHelp": "Each wait is the smaller of initial × 2^(n-1) and the maximum, times 1-r+2r×random; initial equal to the maximum with no jitter means a fixed interval.",
  "resetHint": "\"Reset to default\" removes this route's retryPolicy from the configuration file, restoring the adapter defaults.",
  "summaryRetries": "up to {count} retries",
  "summaryUnlimited": "no attempt limit",
  "summaryFixedDelay": "fixed {delay} ms",
  "summaryBackoffDelay": "{initial} → {maximum} ms",
  "summaryJitter": "jitter {ratio}",
  "save": "Save",
  "reset": "Reset to default",
  "cancel": "Cancel",
  "saveFailed": "Could not save (HTTP {status})",
  "conflict": "The configuration changed in another window or by hand; the page reloaded, please try again.",
  "codeUnknownProvider": "That provider is no longer registered.",
  "codeUnknownModel": "That model is no longer in the list this route serves.",
  "codeInvalidModalities": "The input list must be text only, or text and images.",
  "codeNotEditable": "That provider cannot be configured here.",
  "codeUnavailable": "This deployment is missing a required DSH service.",
  "invalidMaxRetries": "Max retries must be an integer no smaller than 0.",
  "invalidInitialDelay": "Initial delay must be a positive whole number of milliseconds no greater than 2147483647.",
  "invalidMaxDelay": "Max delay must be a positive whole number of milliseconds no greater than 2147483647.",
  "invalidOrder": "Initial delay cannot exceed the max delay.",
  "invalidJitter": "Jitter ratio must be between 0 and 1.",
}
