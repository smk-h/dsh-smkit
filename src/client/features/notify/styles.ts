/**
 * The notify feature's stylesheet, as the text the composition layer
 * injects. One part today; see `custom-settings/styles.ts` for how the parts
 * and their order work when there is more than one.
 */

import pageCss from './style/page.css'

/** The feature's own rules, in cascade order. */
export const NOTIFY_CSS = pageCss
