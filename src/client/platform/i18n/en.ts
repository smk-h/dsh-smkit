/**
 * English dictionary for the `platform` locale namespace.
 *
 * Key set must stay identical to `zh.ts`. Only the platform layer's own
 * components read from here (see `zh.ts`).
 */

import type { LocaleDict } from '../types'

export const PLATFORM_LOCALE_EN: LocaleDict = {
  "cancel": "Cancel",
  "delete": "Delete",
  "refresh": "Refresh",
}
